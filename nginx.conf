server {
    listen 80;
    server_name dashboard.srv1110550.hstgr.cloud 72.61.103.174;

    # Increase upload size for Excel files
    client_max_body_size 100M;

    # Frontend (React build)
    root /var/www/dashboard/frontend;
    index index.html;

    # Serve frontend
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # Proxy API requests to FastAPI backend
    location /api {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Timeout settings for large file uploads
        proxy_connect_timeout 600;
        proxy_send_timeout 600;
        proxy_read_timeout 600;
        send_timeout 600;
    }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/json;
}

# Optional: SSL Configuration (after setting up Let's Encrypt)
# server {
#     listen 443 ssl http2;
#     server_name dashboard.srv1110550.hstgr.cloud;
#
#     ssl_certificate /etc/letsencrypt/live/dashboard.srv1110550.hstgr.cloud/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/dashboard.srv1110550.hstgr.cloud/privkey.pem;
#
#     # Same configuration as above
#     client_max_body_size 100M;
#     root /var/www/dashboard/frontend;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api {
#         proxy_pass http://localhost:8000;
#         # ... same proxy settings
#     }
# }
